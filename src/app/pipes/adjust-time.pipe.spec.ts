import { AdjustTimePipe } from './adjust-time.pipe';

describe('AdjustTimePipe', () => {
  it('create an instance', () => {
    const pipe = new AdjustTimePipe();
    expect(pipe).toBeTruthy();
  });
});
