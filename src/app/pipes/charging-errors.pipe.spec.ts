import { ChargingErrorsPipe } from './charging-errors.pipe';

describe('ChargingErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new ChargingErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
