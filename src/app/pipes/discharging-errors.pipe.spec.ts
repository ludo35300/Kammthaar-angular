import { DischargingErrorsPipe } from './discharging-errors.pipe';

describe('DischargingErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new DischargingErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
